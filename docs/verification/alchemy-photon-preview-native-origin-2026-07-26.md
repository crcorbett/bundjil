---
document_type: proof-receipt
lifecycle: evidence
authority: supporting
owner: bundjil-infrastructure-owner
observed_at: 2026-07-26T17:29:53Z
artifact_git_identity: fdba650e66854bd12f70c0b4e6c01e741f310aee
environment: bundjil-photon-preview-native-origin
review_trigger: replace only after a later approved genuine iMessage-origin journey or callback topology change
---

# Alchemy Photon Preview native-origin receipt — 2026-07-26

## First native-origin attempt

Fresh fingerprint-only Photon readback proved the controlled Mac Messages
start identity remained available to Preview and retained its source binding.
The rollout created exactly one temporary shared user in the isolated Preview
project, preserved the adopted Preview user, and read back one assigned-route
fingerprint. The post-create canonical inventory matched digest
`2ad51bba278457ddd9885fa7babdf1dd50bc3a462a147941511f87b31cba60e5`.

One query-controlled proof callback was registered while preserving the stable
callback. Its mode-`0600` binding artifact supplied exactly the four sensitive
Preview variables. Exact source
`9f40dc44ae96433828d6b37bec87cf853c8d3523` produced READY deployment
`dpl_4BjAP2bNCAyoc44y4SRgNiJin8TP`, and the protected branch alias accepted a
valid-signature unsupported event with an empty `204`.

## Genuine-origin gate

The Mac start identity and assigned route were resolved only in secure process
custody and matched their approved fingerprints. Messages committed the exact
recipient but advertised only `Text Message • SMS`; no text was entered or
sent. The operator then tried the separately approved native Messages service
path. Bounded AppleScript and JXA queries returned no service count or buddy
before timeout.

Read-only System Settings inspection showed the actual `node` invoker's narrow
Messages Automation permission already enabled. ChatGPT's separate Messages
toggle was off. It did not own the node invocation, so it was not changed. No
Full Disk Access, Accessibility, unrelated Automation permission, iCloud
setting or Messages account setting changed.

This directly rejects SMS fallback, a synthetic webhook, a Sendblue-origin
mismatch and an unsupported native-service inference as plausible false
greens. No message, Photon event, provider retry, Eve dispatch/completion,
outbound response, typing transition or handset delivery occurred.

## Exact rollback and readback

The stable callback values were rebound before cleanup. Exact source
`fd630e98e18207c9765d7e4854e14dc1d2e29f90` produced READY deployment
`dpl_4aa6EGdJ7NTnYQjBDdKgJj2HjPNR` with no alias error. Stable-alias signed
probes returned empty `204` responses at `2026-07-26T10:10:34Z` and
`2026-07-26T10:14:22Z`, spanning more than Photon's documented approximately
3.5-minute retry horizon.

Cleanup deleted the proof callback by its exact URL and read back zero matching
callbacks. It then deleted the exact temporary shared user by stable provider
ID through the Photon owner service and read back the adopted user's stable ID
and assigned identity unchanged. Two final canonical inventories matched the
original digest
`9e6108d55bd6801b1d7e041d98cfbdce4587f39c0d0d3384ffad7bc2f7488a3f`.
Final topology is one adopted Preview shared user, one stable webhook, and
unchanged source bindings. Both temporary mode-`0600` artifacts were removed;
stable credentials remain only in ignored mode-`0600` `.env.local` and
Vercel's sensitive Preview metadata.

## Outbound-first handshake attempt

Fresh official Photon documentation and the repository boundary agree that an
outbound direct Space send may establish a conversation, but that Photon
webhooks emit inbound messages only and do not echo outbound sends. The second
attempt therefore retained the signed inbound callback as the only ingress
oracle and treated outbound acceptance or Messages appearance as insufficient.

The canonical baseline inventory matched the original digest
`9e6108d55bd6801b1d7e041d98cfbdce4587f39c0d0d3384ffad7bc2f7488a3f`.
Fresh source and adopted-user readback passed before Photon created exactly one
new temporary Preview user for controlled candidate fingerprint
`82ac258dac9ee2fbdf7430c0e8a1177433ea236c06cd045ffe8683af1a0cc4c5`.
Its stable user fingerprint was
`9e0551a6da4591c12cbaee925ba3d1f60e56b4ad6a1d2964ce790de430a75efc`,
and its assigned-route fingerprint remained
`0809669fba7cf6164486add33143457d5a1c3120ef91b937a4c98e69663200c3`.
Two post-create inventories matched digest
`e4ab75194439bcf5d61a0ffc8ac2a5eaf3955a807da4dce82bf3a1e8c0a090a4`
while preserving the source and adopted Preview bindings.

One query-controlled proof callback was registered while preserving the stable
callback, and the four sensitive Preview values were rebound through the
existing cutover owner. Exact source
`d30509172a2810c2a3e27b893dffc63a0fd0c569` produced READY deployment
`dpl_2GPBK8DJqTAc3M6AQrdkjcfCokP4`; its protected stable alias returned a
valid-signature unsupported-event `204` with no body or redirect.

The existing Photon client acquired its scoped SDK, resolved the direct Space
for the exact registered recipient, and attempted one bounded non-sensitive
send. Photon denied that send with `AuthenticationError`, provider code
`internalError`, transport status `7` (`PERMISSION_DENIED`), and
`retryable=false`. The wrapper preserved the uncertain-delivery disposition
and required readback. No retry occurred and no provider-acceptance marker was
written.

Computer Use then searched Messages for the exact assigned route from secure
process custody. No exact conversation appeared; the route existed only in the
search field. The search was cleared and the UI restored. Because no
conversation existed, there was no iMessage-labelled composer and no reply was
entered or sent. This rejects outbound API invocation, direct-Space
resolution, an uncertain send, a search-field match, SMS fallback and a
synthetic callback as plausible ingress false greens.

## Latest exact rollback and readback

The stable four-value callback binding was restored before cleanup. Exact
source `fdba650e66854bd12f70c0b4e6c01e741f310aee` produced READY deployment
`dpl_4bsT55Mgt52JEswwzzTEY3LfWhwN` with no alias error. Protected
valid-signature stable-alias probes returned empty `204` responses after
deployment readiness. The stable rebind preceded the final probe by more than
the documented approximately 3.5-minute retry horizon.

Cleanup deleted only the proof callback by its exact URL and read back zero
matching callbacks. It deleted only temporary user fingerprint
`9e0551a6da4591c12cbaee925ba3d1f60e56b4ad6a1d2964ce790de430a75efc`
by stable provider identity, then read back adopted user fingerprint
`46b1fb0c12007dbe62245ac934288aa6e189b304423c994edd8e1e40008867bd`
unchanged. The final canonical two-pass inventory matched the original digest
`9e6108d55bd6801b1d7e041d98cfbdce4587f39c0d0d3384ffad7bc2f7488a3f`
and preserved both source bindings. Provider inspection reports shared
iMessage enabled, zero dedicated lines, one adopted Preview user and one
stable callback. The proof callback, temporary user and all three temporary
mode-`0600` custody artifacts are gone.

## Current limitation

The isolated lifecycle and exact rollback are proved, but the required real
Channel journey is blocked at Photon's outbound permission boundary. The
Preview credential can authenticate management, acquire the SDK and resolve
the exact direct Space, but the provider currently denies the send before it
can establish an iMessage-return conversation. The smallest external
prerequisite is Photon enabling or correcting outbound iMessage send capability
for this exact isolated Preview project credential, or issuing a
provider-supported outbound-capable credential scoped to that same project.

No inbound Photon event, provider retry, Eve dispatch/completion, outbound
agent response, typing transition, delivery or handset behavior is claimed.
Later stable-binding, Production and terminal SPEC tasks remain open. This
receipt is not the terminal five-pass audit and makes no Production claim.

## Sources

- [Photon webhook delivery and retries](https://photon.codes/docs/webhooks/delivery)
- [Photon webhook events](https://photon.codes/docs/webhooks/events)
- [Photon Spaces and users](https://photon.codes/docs/spectrum-ts/spaces-and-users)
