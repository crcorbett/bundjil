---
document_type: execution-plan
lifecycle: historical
authority: supporting
owner: bundjil-repository-owner
created: 2026-08-01
completed: 2026-08-01
last_reviewed: 2026-08-01
review_trigger: accepted-finding status, lifecycle owner, receipt identity, package export, README/runbook boundary, verification, or terminal-audit change
audit: ../../documentation-audit/bundjil-structural-audit-2026-08-01.accepted-findings.json
target_revision: 4fc8ba1750524581c281ad97351bf0b1e6e29631
---

# Structural audit corrections

## Outcome and boundaries

Implement the accepted structural corrections from the revision-bound audit in
small dependency-aware slices. This plan owns repository implementation and
local proof only. It authorises no Vercel, Photon, Sendblue, Executor,
credential, deployment, billing, Preview, Production, push, or merge action.

The work starts on isolated branch `codex/structural-audit-corrections` at the
audited target. Alchemy terminates as `implemented`; Hosted Eve terminates as
`implemented` at the SPEC layer with a `completed` task ledger, while its
retained Production qualification remains inconclusive. Neither transition
reopens historical implementation or provider intent.

## Accepted-finding task order

| Task                                           | Finding               | Status    | Dependency and direct postcondition                                                                                                                                                                        |
| ---------------------------------------------- | --------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `reconcile-terminal-lifecycle`                 | `FINDING-001`         | Completed | Both workstreams now have terminal SPEC/ledger/index/plan routing. The focused documentation-policy suite passed 8 tests, including both new adversarial cases, and `bun run check:docs` passed 296 files. |
| `bind-main-integration-receipt`                | `FINDING-002`         | Completed | The dated receipt binds merge `4fc8ba1` to ordered parents `f301722`, `a270116` and tree `485c526`, separates candidate proof from post-merge Git-only readback, and passes 10 focused policy tests.       |
| `retire-root-vercel-brands`                    | `FINDING-003`         | Completed | `/vercel` is the sole public owner; root absence and nominal distinction compile proofs pass, the explicit export conditions are unchanged, and the exact retired-brand scan is empty.                     |
| `restore-readme-runbook-ownership`             | `FINDING-004`         | Completed | Both package READMEs are concise boundary maps with one runbook pointer; only the unique Photon candidate-inventory contract moved to its owning app runbook.                                              |
| `remove-journey-count-prose`                   | `FINDING-006`         | Completed | Current routers and diagnostics are count-free; the canonical ID registry remains the sole inventory owner and the missing-inventory negative passes.                                                      |
| `investigate-authority-artifact-consolidation` | `FINDING-005`         | Deferred  | Reinspection found no callback-free common contract across two current callers; the dated decision preserves all call-site owners and names the exact resume trigger.                                      |
| `terminal-five-pass-audit-and-closeout`        | all accepted findings | Completed | The fresh Sol audit passed all five lenses with no corrections, and full repository verification passed on the receipt-bearing tree.                                                                       |

## Docs-maintainer impact ledger

| Surface                        | Decision                        | Earliest owner, trigger, proof, and claim boundary                                                                                                                                                                                                                                                                                                     |
| ------------------------------ | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Docs and routers               | Change required                 | FINDING-001 moved Hosted Eve to implemented history; FINDING-003 corrected the Vercel identity sample. FINDING-006 makes current journey routes count-free, and the documentation-audit index routes the structural audit plus its FINDING-005 defer decision.                                                                                         |
| READMEs                        | Change required                 | FINDING-004 leaves each package README with purpose, explicit exports, supported checks/public command names, claim limits, and exactly one app-runbook pointer. No procedure/current provider actuality remains. No new prose detector: this is one simultaneous audit finding, not demonstrated recurrence, and keyword detection would be brittle.  |
| Architecture and standards     | Preserve                        | Existing helper-admission rules already permit demonstrated reusable security policy and reject generic buckets. FINDING-005 found no narrower qualifying API, so no durable architecture change is warranted.                                                                                                                                         |
| Runbooks                       | Change required narrowly        | Alchemy already owned every removed infrastructure procedure. Photon alone receives the unique candidate-inventory double-read/digest and blocked-output contract, retaining task authority, no-write, retention, and current-truth limits.                                                                                                            |
| Proof and dated evidence       | Change required                 | FINDING-002 binds exact merge identity and phased checks. FINDING-005's dated decision records seven inspected consumers, common/variable policy, callback risk, supporting tests, preserve boundary, limitations, non-claims, and exact resume trigger. The structural closeout receipt records the five-pass audit and full repository verification. |
| Skills and agent instructions  | Preserve                        | The required local skills and `AGENTS.md` already route the work correctly; `check:skills` proves their unchanged contract.                                                                                                                                                                                                                            |
| Lint, config, CI, and commands | Change required narrowly        | FINDING-006 makes journey-count diagnostics derive from `expectedJourneyIds`; the exact identity oracle remains unchanged. Focused verification policy passed 12 tests and `check:verification` passed the current registry.                                                                                                                           |
| SPECs, tasks, and plans        | Change required                 | FINDING-001 is complete: Alchemy remains implemented with a completed ledger; Hosted Eve is implemented with a completed ledger, accepted Preview history, and inconclusive combined Production retained as a limitation. This completed plan is historical evidence only.                                                                             |
| Tests and fixtures             | Change required                 | Earlier slices own lifecycle, receipt, compile, and inventory negatives. FINDING-005 preserves the eight named capability/codec test owners and adds no speculative cross-caller fixture without a candidate API; structured audit validation passes.                                                                                                  |
| Package config and exports     | Change required                 | FINDING-003 removes only duplicate root Vercel ownership. FINDING-005 preserves every authority call site, Photon locality, and all package exports; no `utils`, `common`, `shared`, new package, or public export is admitted.                                                                                                                        |
| Lifecycle                      | Change required                 | FINDING-001 terminal workstream states and routing agree. FINDING-005's investigation is terminal as Deferred and remains excluded from the accepted crosswalk; the terminal audit and repository verification are complete.                                                                                                                           |
| Release and rollback           | Preserve                        | No release occurs. Repository rollback is ordered reversion of the local correction commit(s); external rollback is N/A.                                                                                                                                                                                                                               |
| Critical journeys              | Preserve identities and oracles | FINDING-006 removes only duplicated counts. `critical-journeys.json`, the command map, `expectedJourneyIds`, exact full-ID oracle, historical receipts, and provider non-claims remain unchanged; current-router count scan is empty.                                                                                                                  |

## FINDING-005 investigation boundary

The completed
[decision](../../documentation-audit/structural-audit-authority-artifact-decision-2026-08-01.json)
defers consolidation. The seven infrastructure consumers share mode-`0600`,
JSON, envelope, and safe-output policy but differ in 64 KiB/2 MiB bounds,
raw-byte fingerprinting, task Schemas, conflict sets, output topology, return
shapes, and error mapping. A common API would require security-sensitive
callbacks that widen the surface. Resume only when two current callers share
the complete callback-free contract named by the decision. Photon remains
package-local and no `utils`, `common`, `shared`, new package, or public export
is admitted.

## Verification and closeout

Run the smallest focused check after each slice. Closeout requires direct docs,
skills, authority, controls, verification, boundaries, Effect setup,
format/lint, Knip, typechecks, package tests/builds, and `bun run verification`.

After all accepted tasks are terminal, run one five-pass audit and record:

1. ownership and call graph;
2. Effect and provider quality;
3. lifecycle and security correctness;
4. verification and adversarial coverage; and
5. documentation, authority, evidence, rollback, and closeout routing.

Any correction found by that audit reopens its owning task and invalidates the
affected receipt until focused and full proof pass again.

The fresh independent Sol review found no correction across the five required
passes. The dated
[closeout receipt](../../documentation-audit/structural-audit-corrections-2026-08-01.receipt.json)
records that result. Full `bun run verification` then passed with only the
documented process-only synthetic Executor fixture; it made no Executor or
other provider request.
